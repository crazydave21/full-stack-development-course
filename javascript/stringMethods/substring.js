let string = "Hello World";

//leaves string at position 1 and extracts remaining strings
console.log(string.substring(1)); // ello World

//leaves string at position 1 and extracts remaining string excluding end position
console.log(string.substring(1, 2)); // e