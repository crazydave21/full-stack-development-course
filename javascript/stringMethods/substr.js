let string = "Hello";

//leaves string at position 1 and extracts remaining strings
console.log(string.substr(1)); // ello

//starts after position 2 and extracts strings from next 2 positions
console.log(string.substr(2, 2)); // ll